// ARRAYS
// upward slash - | | | | |
let myStop: string[] = [ "https://www.youtube.com/watch?v=BBpAmxU_NQo&t=250s", "22:06" ];

// example
const numbers: number[] = [0, 0, 0];
numbers[0] = 10;
numbers[1] = 20;
numbers[2] = 30;
// console.log(numbers);
// console.log(numbers.length);

// example
const numbers2: number[] = [];
numbers2.push(10);
numbers2.push(20);
numbers2.push(30);
numbers2.pop();
// numbers2.findIndex(); // not works
console.log(numbers2);

// exercise
const numbers3: number[] = [];

// myStop = [ "https://www.youtube.com/watch?v=BBpAmxU_NQo&t=250s", "32:01" ];
