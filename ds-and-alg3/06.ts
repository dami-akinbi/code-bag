// Overview of Quicksort

// example NOT DONE
// const array: number[] = [3, -2, -1, 0, 2, 4, 1];
// function qs(array: number[], l: number, r: number) {
//     if (l >= r) return '';
    // let p = partition(array, l, r);
    // qs(array, l, p - 1);
    // qs(array, p + 1, r);
// }

// example NOT DONE
// let array: number[] = [2, 1, 4, 13, 14, 12, 3, 16, 5, 2, 10];
// array = [-2, 3, -1, 5, 4, -3, 0];
// function partition(arr: number[], l: number, r: number) {
//     let pivot = arr[r];
//     let i = l - 1;
//     for (let j: number = l; j <= r - 1; j++) {
//         if (arr[j] < pivot) {
//             i += 1;
            // swap arr[i] and arr[j]
        // }
    // }
    // swap arr[i + 1] and arr[r]
// }
